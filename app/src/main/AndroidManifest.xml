<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission
        android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:label="Pulse Visualizer"
        android:theme="@style/Theme.PulseVisualizer"
        android:supportsRtl="true">


        <!-- Main application -->

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>


        <!-- Full-screen visualizer -->

        <activity
            android:name=".VisualizerActivity"
            android:exported="false" />


        <!--
            Media / Notification Listener

            IMPORTANT:
            This is the existing listener used by the
            project. Do NOT replace it with a second
            MusicNotificationListener service.
        -->

        <service
            android:name=".MediaListenerService"
            android:label="Pulse Visualizer Media Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">

            <intent-filter>

                <action
                    android:name="android.service.notification.NotificationListenerService" />

            </intent-filter>

        </service>


        <!-- Music widget -->

        <receiver
            android:name=".MusicWidgetProvider"
            android:label="Pulse Music"
            android:exported="true">

            <intent-filter>

                <action
                    android:name="android.appwidget.action.APPWIDGET_UPDATE" />

            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/music_widget_info" />

        </receiver>


        <!-- Lyrics widget -->

        <receiver
            android:name=".LyricsWidgetProvider"
            android:label="Pulse Lyrics"
            android:exported="true">

            <intent-filter>

                <action
                    android:name="android.appwidget.action.APPWIDGET_UPDATE" />

            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lyrics_widget_info" />

        </receiver>

    </application>

</manifest>
